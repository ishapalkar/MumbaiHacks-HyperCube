# TokenTrust Agentic AI System Environment Configuration

# Required: Groq API Key for LLM processing
# Get your API key from: https://console.groq.com/
GROQ_API_KEY=your_groq_api_key_here

# Optional: MongoDB for transaction logging and analytics
# If not provided, the system will work without persistent logging
MONGO_URI=mongodb://localhost:27017

# Optional: Redis for device history caching
# If not provided, the system will work without device history
REDIS_URL=redis://localhost:6379

# Server Configuration
PORT=8000

# Optional: Logging Level
LOG_LEVEL=INFO

# Optional: Security Settings
JWT_SECRET=your_jwt_secret_here
ENCRYPTION_KEY=your_encryption_key_here

# Optional: External Service URLs (for production integrations)
NOTIFICATION_SERVICE_URL=https://api.notifications.example.com
MERCHANT_API_BASE=https://api.merchants.example.com
TOKEN_SERVICE_URL=https://api.tokens.example.com